module.exports = [
    {
        sshAddress: '',                   /* git ssh地址 */
        // branch: 'master',              /* clone的分支，默认master */
        // skipInstall: true,             /* 是否跳过install依赖，默认为true */
    },
    {
        sshAddress: '',                   /* git ssh地址 */
        // branch: 'master',              /* clone的分支，默认master */
        // skipInstall: true,             /* 是否跳过install依赖，默认为true */
    },
]