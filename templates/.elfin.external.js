module.exports = [
    {
        sshAddress: '',                   /* git ssh地址 */
        // branch: 'master',              /* clone的分支，默认master */
        // skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
    {
        sshAddress: '',                   /* git ssh地址 */
        // branch: 'master',              /* clone的分支，默认master */
        // skipInstall: false,             /* 是否跳过install依赖，默认为false */
    },
]